#ifndef __MODELS_H_INCLUDED__   // if x.h hasn't been included yet...
#define __MODELS_H_INCLUDED__   //   #define this so the compiler knows it has been included

enum Commands { WHO, SEND, CREATEG, JOING, SENDG, EXIT, UNKNOWN };


#endif 
